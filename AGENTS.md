# Codex 工作约定

## 日 changelog 与周 changelog

日记录和周记录是两套不同粒度的文档，不能混用。日 changelog 是当天开始时建立的持续同步目标：

- 当用户在当天开始时要求创建/启用当天文件（例如 `changelog_8_13.md`），立即使用 `changelog/changelog_8_13.md`；不存在时创建。当天后续每完成一组修改，都必须主动同步这个文件，不需要用户重复提醒。
- 每次同步记录“当前最终变动”：按业务能力、页面行为、数据/状态影响、部署和验证结果归纳。不要写成“改了哪些文件”的流水账，也不要记录中间尝试、临时排查、被后续修改替代的方案。
- 如果后续修改修正或覆盖了当天早先的记录，应直接编辑/合并已有条目，使日 changelog 始终描述最终状态，而不是无条件追加重复条目。
- 用户没有明确创建/启用当天文件时，不要擅自创建日期文件，也不要自动修改周 changelog。
- 周五或用户明确要求“总结本周”时，才根据本周日 changelog、全部 commit 和工作区最终状态更新周 changelog；周记录必须比日记录更粗，按主题合并，不要逐条复制。
- 用户指定的日期文件名优先；例如用户说“创建 `changelog_8_13.md`”，就使用 `changelog/changelog_8_13.md`。

开始整理日或周 changelog 前，必须全面检查：

- `git log` 中当天的 commit、提交信息和变更统计；
- 当前工作区的 `git status` 和未提交 diff；
- 当周已有的 `changelog/` 文件，理解此前已经记录的内容，避免重复或漏记；
- 与本次修改相关的测试、部署脚本、README 和配置文件。

记录规则：

- 日 changelog 追加到用户指定的日期文件；周 changelog 追加到用户指定或仓库当前使用的周文件。
- 以用户可见能力、业务流程、数据/状态影响、部署运维和验证结果为主线归纳；文件名只作为“主要涉及文件”辅助信息，不作为记录主体。
- 同一组相关修改合并记录；不记录普通重启、刷新、临时排查过程。
- 说明主要涉及页面、后端模块、测试和部署文件；涉及发布时记录分支、版本和健康检查结果。
- 如果工作区还有未提交修改，明确区分“已提交/已发布”和“当前未提交”。
- 修改完成后再次用 `rg` 检查过时文件名、过时文档引用和遗漏的当天变更。

## 本仓库发布链路

- 内网发布分支默认是 `20260722`。
- 发布前必须保证本地修改已提交；发布脚本负责推送 GitHub、通知服务器快进拉取并重启/重建容器。
- 内网对外端口是 `8002`；容器内部监听 `8000`，不要把宿主机 `8000` 当作访问地址。
- 部署完成必须检查容器状态和 `http://127.0.0.1:8002/api/health`，不能只依据 Docker 命令退出码判断成功。
